import type { AddressMapProps } from './AddressMap';

// react-native-maps no funciona en web: ahí el punto se fija con "Usar mi ubicación".
export function AddressMap(_props: AddressMapProps) {
  return null;
}
